-- =====================================================================
-- V36: 100편 CineTrip 시드 적재를 위한 movie_region_mappings 초기화
--
-- 사유:
--  - V33 적재 직후의 데모 시드 중 일부가 표준 한국관광공사 광역 area_code
--    (1~8, 31~39) 와 일치하지 않는 값(예: 광주=29, 세종=36, 대구=3)으로
--    들어가 있었다.
--  - CineTripSeedLoader는 `count() == 0` 일 때만 CSV 를 자동 적재하므로,
--    확장된 100편 시드가 반영되려면 기존 매핑을 한 번 비워야 한다.
--
-- 운영 환경 영향: 큐레이션 매핑은 CSV/관리자 /import 로 복구 가능한 시드
-- 데이터이므로 DELETE 안전. (trending_regions_cache / tour_index_snapshots
-- 는 건드리지 않음)
-- =====================================================================

DELETE FROM movie_region_mappings;
